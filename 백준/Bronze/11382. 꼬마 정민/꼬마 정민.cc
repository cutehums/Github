#include <iostream>

using namespace std;



int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    long long x; long long y; long long z; cin >> x >> y >> z;
    cout << x + y + z;
    return 0;
}

